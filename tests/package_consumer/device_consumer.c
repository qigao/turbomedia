#include <salts_capture.h>
#include <turbo_playback.h>

int main(void) {
    salts_capture_t *capture = NULL;
    turbo_playback_t *playback = NULL;

    return capture == NULL && playback == NULL ? 0 : 1;
}
